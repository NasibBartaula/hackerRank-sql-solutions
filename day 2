select round(sqrt(pow((t.b-t.a),2)+pow((t.d-t.c),2)),4) FROM
(
    select 
min(lat_n)as a,
max(lat_n)as b ,
min(long_w)as c,
max(long_w)as d
from station
) as t
;

select round((abs(min(lat_n)-max(lat_n)))+abs((min(long_w)-max(long_w))),4) from station;

